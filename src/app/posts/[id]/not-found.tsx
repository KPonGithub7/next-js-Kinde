import React from "react";

const NotFound = () => {
    return (
        <div className="w-full min-h-screen flex justify-center items-center">
            Post not found for the id.
        </div>
    );
};

export default NotFound;
